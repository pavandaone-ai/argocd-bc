# Image / registry settings
image:
  registry: quay.io
  org: bcbsfl             # <--- change if your Quay org is different
  name: chatbot-sample    # <--- change to your app name in Quay
  tag: "0.1.0"

  # Secret used to PUSH to Quay (BuildConfig) and PULL from Quay (Deployment)
  registrySecretName: quay-registry-secret

service:
  port: 8080

# Quay dockerconfigjson (base64 of ENTIRE ~/.docker/config.json)
# Run on your machine:
#   cat ~/.docker/config.json | base64 -w0
# Then paste the output below.
quay:
  dockerconfigjson: "PASTE_BASE64_DOCKERCONFIGJSON_HERE"



---new


buildConfig:
  name: bcbsfl-nginx-git-build
  appLabel: bcbsfl-nginx-git

git:
  repoUrl: https://gitlab.bcbfs1.com/n5bp/bcbsfl-nginx-demo.git
  revision: main
  contextDir: .

output:
  image: quay-enterprise.apps.ose4-dev.bcbfs1.com/n5bp/argocd-cicd:pavan
  pushSecret: n5bp-test-pull-secret
