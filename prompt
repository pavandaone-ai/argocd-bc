response, err := client.Do(closetask)
if err != nil {
    fmt.Println("Error:", err)
    return
}

responseBody, err := io.ReadAll(response.Body)
if err != nil {
    fmt.Println("Error reading CTASK response:", err)
}

fmt.Println("CTASK status update:", response.StatusCode)
fmt.Println("CTASK API response:", string(responseBody))

response.Body.Close()
