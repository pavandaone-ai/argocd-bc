    }   // line 192: closes for ns loop

    if app_counter == ns_counter {
        fmt.Println("All deployments handled successfully. Closing CTASK.")
        actions.Change_task_status(session_token, task_slice[i], "3", "")
    } else {
        fmt.Println("One or more deployments failed. Closing CTASK.")
        actions.Change_task_status(session_token, task_slice[i], "3", "")
    }

}   // line 193: closes for i loop
