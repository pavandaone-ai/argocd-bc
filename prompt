oc get nodes -o json | jq '
  [ .items[].status.capacity.cpu | tonumber ] | add as $cpu |
  [ .items[].status.capacity.memory
    | sub("Ki$";"") | tonumber ] | add / 1024 / 1024 as $mem |
  "Total CPU: \($cpu) cores\nTotal Memory: \($mem | floor) GiB"
'
