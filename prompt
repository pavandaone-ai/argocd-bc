if app_counter == ns_counter {
    fmt.Println("All deployments handled successfully. Closing CTASK.")

    actions.Update_task_closure(
        session_token,
        task_slice[i],
        "successful",
        "OpenShift deployment automation completed successfully.",
    )

    actions.Change_task_status(
        session_token,
        task_slice[i],
        "3",
        "",
    )

    parentCRQ := actions.TaskParentMap[task_slice[i]]

    fmt.Println("Closing parent Change Request:", parentCRQ)

    actions.Change_crq_status(
        session_token,
        parentCRQ,
        "3",
    )
}
