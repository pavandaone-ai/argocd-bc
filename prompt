if ns_counter == 0 {
    actions.Addnotes(
        session_token,
        task_slice[i],
        "The namespace prefix does not exist. Please consult with the PaaS Solutions team. Closing task.",
    )

    fmt.Println("The namespace prefix does not exist. Closing CTASK.")

    actions.Change_task_status(
        session_token,
        task_slice[i],
        "3",
        "",
    )

    continue
}
