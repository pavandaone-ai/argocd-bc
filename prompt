if status == "3" && reason == "successful" {
    // keep existing one-call behavior
}

if status == "3" && reason == "unsuccessful" {
    // two-call behavior
    // 1. closure information
    // 2. state = 3
}
