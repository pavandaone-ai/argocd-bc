oc get nodes -o json | jq '
. as $r |
([$r.items[].status.capacity.cpu | tonumber] | add) as $cpu |
([$r.items[].status.capacity.memory | sub("Ki$";"") | tonumber] | add / 1024 / 1024) as $mem |
"Total CPU: \($cpu) cores\nTotal Memory: \($mem | floor) GiB"
'
